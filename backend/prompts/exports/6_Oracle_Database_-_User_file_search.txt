Diagnose the following Oracle Database error or issue using the internal documentation available through file search:

## Error Details
{error_text}

## Requirements
- **Search the uploaded documentation** immediately for relevant Standard Operating Procedures (SOPs), runbooks, and known error databases.
- Identify the root cause, considering Oracle-specific contexts (RAC global cache, Data Guard lag, RMAN catalog issues, etc.).
- Prioritize data safety: ensure the database remains consistent and reliable.
- For each resolution step, provide BOTH an `action` description AND the exact `command` (SQL, RMAN, srvctl, lsnrctl, or shell) to execute. A DBA must be able to copy-paste every command directly into their terminal or SQL*Plus session.
- Assess the severity level (e.g., Instance Crash, Session Termination, Performance Degradation).
- List affected components (e.g., specific Tablespaces, Redo Logs, RAC Instances).
- For each preventive measure, provide BOTH an `action` description AND the exact `command` (e.g., `ALTER SYSTEM`, Metric extension query) to implement it.
- **Cite the specific internal documents and sections** used to formulate your diagnosis and solution.